import { runMirrorsRequestInputsUsingOriginalUrlCase } from "../callback/mirrors-request-inputs-using-original-url.callback";

export function registerMirrorsRequestInputsUsingOriginalUrlCase(): void {
  it("mirrors Request inputs using the original request url", runMirrorsRequestInputsUsingOriginalUrlCase);
}
